# Combat Resolvers 01 — Next Step

Open the pull request from `refactor/combat-resolvers-01` to `main`, bind validation to its immutable head SHA, inspect every required check, and correct failures without masking them.

Only a green additive checkpoint permits Phase B wiring. Merge remains a later decision after the wired runtime itself is validated.
